<?php
	// For each loops only works on arrays
	$names = ["Amy", "John", "Lucah"];

	foreach($names as $name){ //select all values from $names and = them to $name
		echo "$name is here <br>"; //print the store value in $name
	}
?>